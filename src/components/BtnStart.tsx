import React from 'react';
import Button from '@material-ui/core/Button';

export default function BtnStart() {
  return (
    <Button className="btn-action" variant="outlined" color="primary">
      <span>Start</span>
    </Button>
  );
}
